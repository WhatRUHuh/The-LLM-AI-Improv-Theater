import React from 'react';

const AIConfigPage: React.FC = () => {
  return (
    <div>
      <h2>AI 配置</h2>
      <p>在这里配置连接不同 AI 服务商的 API Key、Endpoint 等。</p>
    </div>
  );
};

export default AIConfigPage;